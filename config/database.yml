development:
  adapter: postgresql
  database: sample_sns_development
  username: postgres
  password: oM7euDL5dorpZd9qS5CA9rRhi6gPohsL
  host: 127.0.0.1
  port: 5432



production:
  primary:
    adapter: postgresql
    encoding: utf8
    pool: 5
    host: <%= ENV["RAILS_HOST"] %>
    database: <%= ENV["RAILS_DATABASE"] %>
    username: <%= ENV["RAILS_DATABASE_USERNAME"] %>
    password: <%= ENV["RAILS_DATABASE_PASSWORD"] %>

  queue:
    adapter: postgresql
    encoding: utf8
    pool: 5
    host: <%= ENV["QUEUE_DB_HOST"] || ENV["RAILS_HOST"] %>
    database: <%= ENV["QUEUE_DB_NAME"] || ENV["RAILS_QUEUE_DATABASE"] || "sample_sns_queue_production" %>
    username: <%= ENV["QUEUE_DB_USERNAME"] || ENV["RAILS_DATABASE_USERNAME"] %>
    password: <%= ENV["QUEUE_DB_PASSWORD"] || ENV["RAILS_DATABASE_PASSWORD"] %>

